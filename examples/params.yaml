transit:
  t0: 0.0 # time of inferior conjunction
  per: 2.21857567 # orbital period in days
  rp: 0.1548291121 # planet radius (in units of stellar radii)
  a: 8.9625402483 # semi-major axis (in units of stellar radii)
  inc: 85.71 # orbital inclination (in degrees)
  ecc: 0.0 # eccentricity
  w: 20.0 # longitude of periastron (in degrees)
  u: [0.1, 0.3] # limb darkening coefficients [u1, u2]
  limb_dark: "quadratic" # limb darkening model
classifier:
  type: cnn # [cnn, rf] for Convolutional Neural Network and Random Forest
#assignmnet2 taskG
detection:
  method: rf                 # optional label, not used by code but informative
  kernel: default_rf         # name of the RF configuration
  csv_path: tess_data.csv    # <<< dataset path (required in the assignment)
  n_bins: 1000
  use_scaler: false
  samples_per_class: 350
  n_estimators: 500
  max_depth: null
#task h assignment 2
detection:
  kernel: cnn          # or "rf" when you want RF by default (not used by CLI, but good documentation)
  csv_path: tess_data.csv
  n_bins: 1000
  use_scaler: true
  samples_per_class: 350

  # CNN-specific hyperparameters (Task H)
  epochs: 20
  batch_size: 64
  learning_rate: 0.001
#assignmnet2 Task I
dream:
  dataset_path: "/ca25/comp_astro_25/tess_data.csv"   # path to your lightcurve CSV
  label_column: "label"                    # or remove if there is no label column

  batch_size: 128
  z_dim: 100
  ngf: 32
  ndf: 32
  epochs: 100
  learning_rate: 0.0002
  beta1: 0.5

  model_dir: "models/gan"
  sample_output: "assignment2_taskI_dream.png"
